{
  "name": "expo-agent-cli-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/@expo/*",
    "apps/*",
    "packages/@expo/agent-cli/evals/fixtures/real-app"
  ],
  "scripts": {
    "bundle": "bun run --filter expo-agent-cli bundle",
    "test": "bun test && bun run --filter @expo/agent-cli test"
  },
  "devDependencies": {
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0"
  },
  "patchedDependencies": {
    "@expo/agent-eval-vitest@0.0.1": "patches/@expo%2Fagent-eval-vitest@0.0.1.patch"
  }
}
