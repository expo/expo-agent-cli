{
  "id": "runtime-errors-no-server",
  "fixture": "e2e/fixtures/go-app",
  "taskPrompt": "collect the runtime errors from the running app",
  "drivingAgent": "deterministic",
  "tiers": [0],
  "command": {
    "argv": ["runtime:errors", "--json"],
    "timeoutMs": 60000
  },
  "graders": [{ "type": "exit-code", "expect": 1 }]
}
