{
  "id": "dev-stop-none",
  "fixture": "e2e/fixtures/go-app",
  "taskPrompt": "stop this project's dev server",
  "drivingAgent": "deterministic",
  "tiers": [0],
  "command": {
    "argv": ["dev:stop", "--json"],
    "env": { "LOG_EVENTS": "events.jsonl" },
    "timeoutMs": 60000
  },
  "graders": [
    { "type": "exit-code", "expect": 0 },
    { "type": "jsonl-event", "file": "events.jsonl", "event": "cli:dev_stop" }
  ]
}
